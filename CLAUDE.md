# CLAUDE.md — 프로젝트 작업 규칙

메인 지침서는 `docs/PROJECT_GUIDELINES.md` (v4.0). 이 파일은 **코드/도구 사용 규칙**만 담는다.

## 0. 있는 바퀴부터 찾는다 (최우선·타협 불가)

**코드를 한 줄 쓰기 전에 "이거 이미 있나"부터 뒤진다.** 오너 질책 4회 이상.
같은 계산이 두 군데 있으면 반드시 어긋나고, 어긋나면 오너는 어느 화면을 믿을지 알 수 없다.

### 쓰기 전에 이 순서로 뒤진다

1. **이 저장소부터.** 새 함수를 만들기 전에 무조건 먼저 돌린다:
   ```bash
   grep -rn "핵심단어" src/ api/ --include=*.py      # 같은 계산이 이미 있나
   grep -rn "함수이름\|클래스이름" src/ api/ web/src/  # 누가 이미 쓰고 있나
   ```
   `docs/CODEMAPS/` 와 각 모듈 맨 위 독스트링에 "정본이 어디인가"가 적혀 있다. 그걸 먼저 읽는다.
2. **표준·라이브러리.** TradingView·TA-Lib 등 업계 표준 정의와 기존 구현을 먼저 본다.
   계산식을 **직접 설계하지 않는다**(ADR-0014 지지저항 = TradingView 포팅이 그 예).
3. **GitHub·패키지 저장소.** 그다음이다. 라이선스는 반드시 먼저 확인한다(비상업용 ❌).

### 절대 금지

- **같은 계산을 두 번 구현하는 것.** 화면(③ 시뮬레이션)·엔진(④ 백테스트)·차트 오버레이는
  **같은 함수**를 불러야 한다. 한쪽만 고치면 두 화면이 다른 답을 낸다.
  - 실제 사고 2026-08-22: `/api/simulate` 가 `_run_symbol` 을 안 쓰고 체결 코드를 따로
    갖고 있어서, ④ 백테스트는 "매수 1건 + 손절"인데 ③ 차트는 체결 0건이었다. 그래서
    화면에 매수·매도 화살표가 아예 안 떴다.
- 기존 모듈을 안 보고 "비슷한 걸 새로 만드는 것". 고칠 게 있으면 **정본을 고친다.**

### 다 만들고 나서 반드시 확인

- 같은 값을 두 화면에서 뽑아 **실제로 대조한다**(숫자를 눈으로 맞춘다).
  "고쳤다"는 말은 대조 결과를 붙여서만 한다 — 근거 없이 고쳤다고 하지 않는다.

## MCP 사용 규칙 (타협 불가)

- **MCP는 개발 보조로만 쓴다. 매매 실행 경로(주문·포지션·자금 이동)에 절대 넣지 않는다.**
- 허용: API 문서 검색, 코드 생성 보조, 이슈 관리(Linear 등), 데이터 탐색.
  - 예) KIS Code Assistant MCP = 개발 시점 API 검색 도구로만 OK.
- 금지: LLM/MCP가 BUY/SELL·포지션 크기·손절 라인을 결정하거나 주문을 전송하는 것.
  - `migusdn/KIS_MCP_Server` 등 **조회·주문을 수행하는 서드파티 KIS MCP는 실행 경로에 연결 ❌.**
  - 예외(2026-08-05 오너): KIS 공식 트레이딩 MCP를 개발 세션에 **조회 전용**으로 연결 허용.
    주문·정정·취소 도구 호출 금지, 연결되면 해당 도구를 settings 권한 deny 목록에 등록한다.
  - 나무 공식 MCP(`PLUG-OpenAPI/nhplug-mcp`)도 같은 조건으로만 허용(ADR-0018):
    조회 전용, `NHPLUG_ENABLE_TRADING` 기본 false 유지, 주문 도구는 deny 등록.
- 근거: `PROJECT_GUIDELINES.md` §3.1(Layer 3 매매 = 결정론적 룰), §3.2(LLM은 ordinal 등급까지만),
  README "Group A 함정 #1(잘못된 도구 사용)". 매매 실행은 항상 결정론적 코드가 한다.

## 역할 경계 (타협 불가, ADR-0019)

**내(클로드) 일은 셋뿐이다: 코딩 · (뉴스·재료 수집 시점의) 글 해석 · 시키면 계산 돌리고 보고.**

내가 하지 않는 것:
- 어떤 조건으로 종목을 거를지 **정하지 않는다** — 오너가 화면에서 고른다 (ADR-0009)
- 무엇을 먼저 확인할지 **우선순위를 매기지 않는다**
- 임계값(시총 하한·되돌림 비율·호가 오프셋)을 **고르지 않는다**
- 백테스트 구간을 **정하지 않는다**
- 화면에서 고를 수 있는 것을 파이썬에 **다시 정의하지 않는다** (경로가 둘로 갈린다)

내 일은 **"오너가 그걸 화면에서 고를 수 있게 만드는 것"**이다.
질문할 때도 "무엇을 고를까요?"가 아니라 **"화면에 이 선택지가 없는데 만들까요?"** 로 묻는다.
데이터 사실은 추정해 고치지 말고 실측해 보고한다. 수급의 옛 `0/595` 결손은 전 종목
백필 뒤 `595/595`로 해소됐다(2026-08-29 재확인).

근거: 오너 질책 2026-08-16 — "너는 그냥 코딩, 자연어로 해석 용도고, 내가 필요하면 준비된
데이터에 따라 검증 계산 돌리면 되는 거야. 나머지는 내가 웹에서 조작하면서 알아서 한다고."

## 확정된 기술 결정 (v4.0)

- **정체성 = 확인하는 시스템** (ADR-0019). 트레이더 발언 → 확인할 주장 → 데이터로 확인 →
  전략 → **사람 승인** → 예약 주문 자동 실행 → 힘이 약해지는지 감시.
  AI가 전략을 창조하지 않는다. "영원한 진화" ❌
- **전략은 9단 계층으로 정의**: 종목 선정 → 시장 상황 → 전략군 → 모양 → 진입 → 비중 →
  청산 → 위험 관리 → 실행. 전략 1호 = "상승 사이클 눌림매매"의 **피보나치 진입 모델**
- **확인 안 된 주장은 `docs/hypotheses/`** 에 둔다. 지침서 본문에는 확정된 원칙만

- **데이터 범위 = 최소**: 일별 OHLCV + 거래대금 + 시가총액/상장주식수 + 당시 기준 종목 목록(상폐 포함).
  뉴스·공시·유튜브는 2단계(LLM 도입) 때 추가.
  **실측 범위는 1995~2026** (2026-08-16 확인) — 옛 문서의 "2017-01~현재"는 틀린 서술이었다.
- **가격/시총 데이터 소스**: `FinanceData/marcap` (1995~현재, parquet, 상폐 종목 포함) 우선 검토.
  marcap 뒤쪽 공백(저장소 지연분)은 KRX Open API 일별매매정보로 채운다(`KRX_AUTH_KEY`, 조회 전용,
  날짜당 3콜, ADR-0002 개정 2026-08-18). pykrx 웹 스크래핑은 KRX 로그인 게이트로 깨진 상태 — 신규 의존 ❌.
- **수급(외인/기관/개인) 데이터**: 확정(ADR-0012 수락, 2026-08-15). 백필 정본 = KIS
  투자자매매동향(전 종목 × 1995~, `data/derived/supply/`), 증분·교차검증 보조 = 나무 PLUG.
- **백테스트 엔진 = 자체 "얇게"**. vectorbt/backtesting.py는 P&L 대조 oracle로만. 메인 엔진 ❌.
- **주문 창구 = 나무증권(NH PLUG)** (ADR-0018, 2026-08-15 — 모의 왕복 검증 1건 조건부).
  KIS는 조회 전용으로 병행(과거 수급 백필). 단계 5는 나무 모의투자(moapi, 계좌구분 03).
  단계 6 전까지 실계좌 자금 이동 ❌. 실계좌 주문은 사람이 스크립트로만(namuh_buy_one.py).
- **주·월봉 차트 정본 = 나무 수집본**(`data/derived/namuh_bars/`), 일봉 합성은 상폐·미수집
  종목 대체용. 매일 증분은 KIS(오늘 일봉 = 멀티시세 30종목/콜, 주·월봉 = 기간별시세; 연말 주만
  나무)로 붙인다 — ADR-0018 개정 2026-08-18. 백필 이력·정본 소스는 그대로 나무.
  장중엔 전 종목 갱신 ❌ — 차트 연 종목만 나무 웹소켓으로 오늘 봉 표시(파일 저장 ❌).
  수집 데이터는 재배포 금지 — GitHub 업로드 ❌, 로컬 `data/`만.

## 방법론 가드레일 (백테스트 무효화 방지)

- **망한 회사도 데이터에 남긴다**(상폐 종목 포함) — 빼면 백테스트 수익률이 부풀려진다
  (= 살아남은 것만 보는 착시).
  수급도 전 종목 과거 백필을 마쳐 상폐 표본 595개 모두 자기 거래 기간 자료가 있다
  (2026-08-29). 분석 전 날짜 범위와 가격·거래대금을 표본 대조한다.
- 각 시점에 실제 거래되던 종목만 본다 = **당시 기준 종목 목록**. 데이터 1995~현재.
- **검사는 진입 시점 기준으로 한다.** 오늘 기준으로 과거 종목을 늘어놓으면 이미 결과가 난 걸
  보는 것이지 알아낸 게 아니다. 눌림 매매면 "신고가를 찍은 그날까지의 데이터로 파동을 긋고,
  그 다음날부터 어떻게 됐나"를 봐야 한다.
  근거: 2026-08-07 실수 — 10종목을 오늘 기준으로 보고 "무너진 종목은 시작점 아래로 뚫려
  자동으로 걸러진다"고 했는데, 신고가 시점으로 다시 재니 좋은 종목과 나쁜 종목이 전혀
  구분되지 않았다(무너진 쪽 반등이 오히려 더 컸다). 오너 지적: "신고가였을 때 또는 신고가
  찍고 얼마 안 걸렸을 때를 봐야지."
- **미래 데이터 훔쳐보기 금지.** "신호 계산 시점 < 체결 시점" 불변식을 코드로 강제한다.
  (종가 진입 전제의 ADR-0001은 폐기 — 전략은 미정, 진입 방식은 전략 확정 후 새 ADR로.)
- **검사 구간은 오너가 화면에서 정한다.** 코드가 강제하지 않는다. 기본값 2007-01-01~ 최신 거래일.
  오너 결정 2026-08-16: 2007년부터 나누지 않고 전체 (연습/검증/시험 3분할 ❌).
  최종 확인은 나무 모의투자(단계 5)가 대신한다.
- 신호 채택: 단순 alpha 합산 ❌, drop-one marginal contribution ⭕. WRL과 IC는 별개 지표. N<30 신뢰 불가.
- 모든 정량 임계값은 placeholder. 하드코딩 ❌. 거래비용·슬리피지 처음부터 포함.

## 작업 규칙

- 큰 삭제·구조 변경 전 확인. 기존 디테일·근거는 의심스러우면 보존.
- "~한 거 맞지?" 점검에 자동 yes 금지. 실제 확인 후 답하고, 누락·오류는 인정.
- 한국어로 응답.
- **쉬운 말로 말한다.** 화면 라벨뿐 아니라 오너에게 하는 설명·질문 선택지·문서 전부 해당.
  영어 용어(leg·swing·pivot·drawdown·look-ahead)와 지어낸 용어를 그대로 쓰지 않는다.
  꼭 필요하면 쉬운 말을 먼저 쓰고 괄호 안에만 원어를 넣는다.
  예) 하락 레그 ❌ → "고점 찍고 내려온 구간" ⭕ / 스윙 ❌ → "오르내린 한 구간" ⭕ /
  피벗 ❌ → "방향이 꺾인 지점" ⭕ / 룩어헤드 ❌ → "미래 데이터 훔쳐보기" ⭕
  근거: 오너 질책 2026-08-05, 2026-08-07("이거 쉽게 말하는 법도 좀 원칙에 넣자").

- **영어 직역투로 쓰지 않는다. 한국말처럼 들리게 쓴다.** 쉬운 말로 바꾸는 것만으로는
  부족하다 — 단어를 우리말로 바꿔 놓고 문장 구조가 영어면 여전히 어색하다.
  화면 라벨·버튼·설명문·오류 문구 전부 해당. **소리 내어 읽어서 어색하면 틀린 것이다.**
  예) "고른 날까지" ❌(until the selected date) → "살 때 상황" ⭕ /
  "이 매매" ❌(this trade) → "산 뒤 흐름" ⭕ /
  "데이터 없음" ❌(no data) → "받아온 게 없습니다" ⭕ /
  "갱신 중…" ❌ → "받아오는 중…" ⭕
  특히 **명사만 덜렁 놓는 이름표**를 조심한다 — 한국말은 "무엇을 하는지"가 들어가야 자연스럽다.
  근거: 오너 질책 2026-08-22("명칭 좀 영어 직역투 쓰지 좀 마라", "한국식으로 어색하게
  말하지 말라고").

## Linear 동기화 (매 세션 필수)

Linear 팀 `BORB` / 프로젝트 **`Algo Trading Research System`**.

- **세션 시작 시**: 열린 이슈를 조회해 현재 상태를 파악한다. 오너가 "지난번에 뭐 하고 있었지"를
  다시 설명하지 않아도 되게 한다.
- **작업 종료 시**: 이번 세션에서 끝난 이슈는 `Done`, 새로 생긴 미결은 이슈로 추가한다.
- **어긋나면 보고**: Linear가 ADR·코드와 다르면 **자동으로 고치지 말고 어긋난 지점을 말한다.**
  정본은 항상 `docs/adr/`·`docs/` 다. Linear는 추적용 사본이다.
- **삭제 불가**: 이 MCP에는 이슈·프로젝트 삭제 도구가 없다(첨부·댓글만 삭제 가능).
  잘못 만든 이슈는 `Canceled` 처리하고 오너에게 알린다. 실제 삭제는 오너가 UI에서 한다.
- **연결 안 된 세션**: Linear MCP 인증이 없으면 건너뛰되, **건너뛰었다는 사실을 반드시 말한다.**
  조용히 넘어가면 오너가 최신인 줄 착각한다.
