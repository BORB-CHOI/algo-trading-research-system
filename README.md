# Algo Trading Research System (ATS)

> 한국 주식 스윙 매매 — **확인하고, 사람이 승인하고, 그다음 자동 실행하는** 시스템.
> 트레이더의 말을 데이터로 확인하고, 승인된 계획만 예약 주문으로 내보낸다.

## 이게 뭔가 (한 문단)

**검증된 트레이더(실전 10년+, 큰 자금 운용)의 매매 관점과 발언을 하나하나 확인할 수 있는
주장으로 바꾸고, 한국 증시 과거 데이터로 그게 실제 미래 수익과 관계가 있는지 확인한다.**
시장 상황·종목 상태·매매 유형별로 통하는 전략을 찾고, **사용자가 최종 승인한 매매 계획만**
예약 매수·매도 주문으로 자동 실행하며, 그 전략의 힘이 약해지는지 지켜본다.

**아닌 것:** AI가 알아서 사고파는 자율 자동매매 ❌ · AI가 끝없이 새 전략을 만들어내는 시스템 ❌ ·
증시 전체에 통하는 만능 전략 하나를 찾는 일 ❌

**누가 무엇을 하나:** 사람이 **무엇을 확인할지·조건·임계값·구간을 정하고 최종 승인**한다.
AI는 **코딩 · 글 해석 · 시키면 계산 돌리기** 셋만 한다. 시스템은 데이터·선별·백테스트·
체결 흉내·승인 후 주문 실행을 맡는다. **AI가 BUY/SELL·비중·손절선을 정하지 않는다.**

한국 시장을 고른 이유는 **외인/기관 일별 수급이 공개**되기 때문이다 — 미국엔 없다.

## 디렉토리 구조

```
algo-trading-research-system/
├── README.md                     ← 이 파일
├── CLAUDE.md                     ← 코드·도구 사용 규칙
├── docs/
│   ├── README.md                 ← 문서의 단일 시작점
│   ├── foundation/               ← 확정 원칙·아키텍처·데이터 계약
│   ├── research/                 ← 확인 전 가설·확인 후 결과
│   ├── development/              ← 개발 환경·기여 절차
│   ├── project/                  ← 진행 상황·변경 이력
│   └── adr/                      ← 되돌리기 어려운 구현 결정
├── src/
│   ├── layer1_data/              데이터 수집·정제·저장          (가동 중)
│   ├── layer2_llm_reading/           신호 — LLM 자리                (2단계, 비어 있음)
│   ├── layer3_strategy/          선정·모양·진입·청산            (가동 중)
│   └── layer4_execution/         백테스트·체결·실행             (가동 중)
├── api/ + web/                   케이스 검사기 웹 (ADR-0005·0008)
├── scripts/                      수집·백필 도구
└── tests/                        618개
```

## 지금 어디까지 왔나

| 단계 | 작업 | 상태 |
|---|---|---|
| 1 | 데이터 파이프라인 | 가동 중 |
| 2 | 백테스트 엔진 + 종목 선정 = **1단계(정량만)** | 가동 중 |
| 3 | LLM 신호 모듈 | 대기 |
| 4 | 신호 + 매매 룰 = **2단계(LLM 추가)** | 대기 |
| 5 | 나무 모의투자 | 대기 |
| 6 | 실거래 (100만원) | 대기 |

**기한 없음.** 프로젝트가 아니라 시스템이다.

### 데이터 (실측 2026-08-16)

| 데이터 | 범위 | 종목 |
|---|---|---|
| 가격·시총·거래대금 (marcap) | **1995 ~ 2026** | — |
| 수정주가 일봉 | 1995 ~ | **5,478** (상장폐지 포함) |
| 수급 (외인/기관/개인) | 1995 ~ | **5,478** (상폐 포함, 2026-08-29 백필 확인) |
| 주·월봉 (일봉으로 만듦) | 일봉과 같음 | 4,310 |
| 분봉 1~240분 (키움 1분봉 + 만든 굵은 봉) | 굵기마다 다름 (2026-08-30) | 4,310 |

## 실행 (케이스 검사기 웹 도구, ADR-0005)

터미널 두 개를 리포 루트에서 띄운다.

```bash
make api   # 백엔드 — FastAPI :8000
make web   # 프런트 — Vite :5173
```

- 백엔드를 빼먹으면 화면은 뜨지만 차트가 빈다. 프런트는 parquet 을 직접 못 읽어서
  `/api/*` 를 FastAPI 로 proxy 하기 때문(`web/vite.config.ts`).
- 가상환경을 활성화하지 않아도 된다. Makefile 이 `.venv/bin/` 을 직접 부른다.
  경로가 다르면 `make api VENV=~/myenv`.
- 풀어 쓰면 `uvicorn api.main:app --reload --port 8000` / `cd web && npm run dev`.
- 이 서버는 **데이터를 보여주기만** 한다. BUY/SELL·포지션·주문은 여기 없다(CLAUDE.md).

나머지 명령은 `make help`.

## 핵심 원칙 (다섯 가지만)

1. **역할 경계** — 사람이 정하고 승인한다. AI는 코딩·글 해석·시키면 계산.
   **AI가 조건·임계값·구간을 정하지 않는다**
2. **그때 알 수 있었던 정보만** — 가격·시총·수급·뉴스·신고가 전부.
   "신호 계산 시점 < 체결 시점"을 테스트로 강제한다
3. **당시 기준 종목 목록** — 망한 회사를 데이터에 남긴다.
   빼면 수익률이 부풀려진다 (살아남은 것만 보는 착시)
4. **4-layer 분리** — LLM은 layer2 까지만. 매매 결정(layer3)은 언제나 결정론적 코드
5. **살아남는 것이 먼저** — 미수·신용·스탁론 ❌.
   매매 안 하면 죽지 않는다. 안전장치는 돌아가는 중에 못 바꾼다

문서는 [docs 안내](docs/README.md)에서 목적에 따라 찾는다. 확정 원칙의 정본은
[PROJECT_GUIDELINES](docs/foundation/PROJECT_GUIDELINES.md)다.

## v4.0에서 바뀐 것 (2026-08-16)

옛 정체성 *"조건 라이브러리를 쌓아 영원히 진화하는 자동매매 시스템"* 을 폐기했다.
실제로 하고 있는 일은 진화가 아니라 **확인**이기 때문이다.

- **정체성 재정의** — 확인하고, 사람이 승인하고, 그다음 자동 실행 ([ADR-0019](docs/adr/0019-ats-identity-v4.md))
- **전략 9단 계층 도입** — 종목 선정 → 시장 상황 → 전략군 → 모양 → 진입 → 비중 → 청산 → 위험 → 실행
  - "전략 1호 = 피보나치"가 아니라 **상승 사이클 눌림매매의 진입 방법 하나가 피보나치**.
    격하가 아니라 승격 — 형제 진입 방법을 끼워 넣을 자리를 만든 것. **코드는 그대로**
- **확인 안 된 주장을 분리** — 조건 35개·재료 5축·회피 6가지·트리거를
  `docs/research/hypotheses/`로 이관.
  v3.8 원본은 2026-08-23 저장소 정리로 삭제됐고 git 이력에 보존
- **역할 경계 명문화** — AI가 조건·임계값·우선순위·구간을 정하지 않는다
- **구간 강제 제거** — 화면에서 고른 날짜가 그대로 검사 구간. 기본값 2007~ (리먼 전부터)
- **데이터 범위 정정** — "2017~" 은 틀린 서술이었다. 실제 **1995~**
- **수급 결손 해소** — 처음에는 상장폐지 표본 595개 중 0개였으나 전 종목 과거 백필 뒤
  595개 모두 자기 거래 기간 수급을 갖는다(2026-08-29 재확인)

### 용어

| 옛 표현 | v4.0 |
|---|---|
| look-ahead | 미래 데이터 훔쳐보기 |
| point-in-time universe | 당시 기준 종목 목록 |
| survivorship bias | 살아남은 것만 보는 착시 |
| 조건 라이브러리 | 확인 대기 원재료 |
| 영원한 진화 | 힘이 약해지면 알려 주기 |

## 흔한 LLM 트레이딩 실수 — 이 시스템이 피하는 함정

학계·업계 LLM 트레이딩 시스템이 반복해 빠지는 함정 20가지를 5개 묶음으로 정리한 문서가
삭제된 v3.8 원본 README에 있었고, 필요하면 git 이력에서 확인할 수 있다.
요지는 다섯 줄로 줄어든다.

- **A. 잘못된 도구** — LLM에게 매매·예측을 시킨다 → LLM은 layer2 까지, 등급까지만
- **B. 시장 메커니즘 무시** — 결과가 아니라 예상과의 차이가 가격을 움직인다 →
  `docs/research/hypotheses/news.md`
- **C. 통계 부재** — 같은 구간 보며 값 고치기, 표본 부족, 조건 강화 후 지표 확인 안 함 → §4
- **D. 위험 관리 실패** — 단일 알파 · 풀매수 · 손절 못함 · 거래비용 무시 → §7
- **E. 사람 심리** — 개입 · 과로 · AI 환상 · 데이트레이딩 → §8.1 장중 무개입

## 새 채팅 시작 방법

1. `docs/README.md`에서 작업에 필요한 문서를 찾는다
2. 확정 원칙이 필요한 작업이면 `docs/foundation/PROJECT_GUIDELINES.md`를 컨텍스트로 준다
3. 작업 명시: *"이 지침서 기반으로 [구체 작업] 진행"*
4. 한 채팅 = 한 작업
5. 합의된 변경만 정본 문서에 반영 + Git commit

## 버전 관리

- `docs/foundation/PROJECT_GUIDELINES.md`는 **확정된 원칙만** 담는다. 확인 안 된 주장은
  `docs/research/hypotheses/`에 둔다
- 되돌리기 어려운 결정은 `docs/adr/` 에 한 건씩. **뒤집을 때 지우지 않고 상태만 바꾼다**
- 변경 이력은 `docs/project/CHANGELOG.md`
- 머릿속 시뮬레이션으로 지침서 갱신 ❌ — 실제 구현·데이터·실거래 경험 기반으로만
