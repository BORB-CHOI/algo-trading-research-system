# 데이터 스키마

> **상태: 가격/시총 = 확정** (2026-07-20 실측 대조 완료, 2017~2026 604만 행).
> 수급 부분은 소스 미정(ADR-0002)이라 TBD.
>
> 초안은 marcap README 기준으로 썼는데 **실측과 여러 곳이 달랐다** — 타입(int → float),
> 오타 컬럼(`ChagesRatio`) 존재 여부, `Marcap` 단위. 아래는 전부 실측 기준으로 정정한 값이다.

## 1. 가격/시총 패널 — `FinanceData/marcap`

일별 전종목 스냅샷. `Date`를 인덱스로, 그날 상장돼 있던 모든 종목이 한 행씩. **상장폐지 종목은
상폐 전까지 자기 날짜에 존재** → 망한 회사가 데이터에 남고(살아남은 것만 보는 착시 = survivorship
bias 방지), 각 시점에 실제 거래되던 종목만 보이는 것(point-in-time universe)을 데이터가 보장.

| 컬럼 | 의미 | 타입 | 백테스트 용도 |
|------|------|------|--------------|
| Date | 날짜 | datetime64[ns] | 시계열 키 |
| Code | 종목코드 | str(6) | 종목 키 — **로더가 `zfill(6)` 보정**(아래 주의) |
| Name | 종목명 | str | 표시용 (가명화 대상, Phase 2) |
| Open/High/Low/Close | 시/고/저/종가 | float64 | 진입가(종가), 변동성 |
| Volume | 거래량(주) | **float64** | — |
| **Amount** | **거래대금(원)** | **float64** | **거래대금 z-score(§4.3), 유동성 필터** |
| Changes | 전일대비 | float64 | 등락 |
| ChangeCode | 등락 기호 | str | — |
| ChangesRatio | 전일대비 등락률(%) | float64 | 등락률 필터(§4.2) |
| **Marcap** | **시가총액(원)** | **float64** | **시총 필터(§4.2)** |
| **Stocks** | **상장주식수** | int64 | 유통 규모 |
| MarketId | 시장기호 | str | — |
| Market | 시장 | str | `KOSPI` / `KOSDAQ` / `KOSDAQ GLOBAL` / `KONEX` |
| Dept | 거래소 소속부 | str | **관리종목·스팩 판별** (아래) |
| Rank | 시총 순위 | int64 | — |

### Dept — 거래소가 직접 표시하는 상태값 (2017~2026 실측)

이름 추측보다 정확하다. `exclusions.py` 가 이 값으로 관리종목·스팩을 가른다.

| 값 | 행 수 | 비고 |
|---|---:|---|
| (결측) | 2,167,931 | KOSPI 는 대부분 비어 있다 |
| 중견기업부 / 우량기업부 / 벤처기업부 / 기술성장기업부 / 일반기업부 | 3,480,702 | 정상 |
| `SPAC(소속부없음)` | 146,118 | **제외 대상** |
| `관리종목(소속부없음)` | 124,033 | **제외 대상** |
| `투자주의환기종목(소속부없음)` | 86,874 | **제외 대상** |
| `외국기업(소속부없음)` | 41,879 | 미정 |
| 크라우드펀딩기업부 / 스타트업기업부 | 858 | KONEX |

> 결측이 두 종류다 — 진짜 `NaN` 과 **문자열 `"nan"`**. `isna()` 만으로는 안 걸러진다.

### 주의 — 종목코드 앞자리 0 (2000-05-29 이전)

marcap 은 6자리 코드 체계 도입(2000-05-29) 이전 코드를 **숫자로 저장**해 앞자리 0 이 날아가 있다
(삼성전자 `005930` → `"5930"`). 보정 없이 쓰면 코드 개편일에 1,460개 종목이 한꺼번에
상장폐지된 것처럼 보인다. `load_years()` 가 `zfill(6)` 으로 복원한다 (ADR-0003).
백테스트 구간(2017~)은 전부 6자리라 영향 없으나, **구간을 과거로 넓히면 반드시 걸린다.**

**검증 완료 (2026-07-20, `tests/test_marcap_loader.py`):**
- ✅ 망한 회사가 데이터에 남아 있는가 — 2017~ 구간 564개, 연도별 31~82개 고른 분포.
- ✅ `Marcap == Close × Stocks` — 604만 행 전부 일치, 불일치 0건. **단위는 원**
  (초안의 `/ 1e6`, 백만원 기재는 오류였다).
- ✅ 필수 컬럼 결측 0, `(Date, Code)` 중복 0, 연간 거래일 242~248일.
- ✅ 종목코드 6자리 정규화.

**점검 완료 (2026-07-24, BORB-32 전수 조사 1995~2026, 15,499,054행 5,474종목):**
- **ETF/ETN 미포함.** `KODEX/TIGER/ARIRANG/…/ETN` 이름 패턴 전수 매칭 결과 실제 ETF/ETN 0건
  (매칭 20건은 전부 "파워"가 든 일반 기업 — 보성파워텍 등). `Market` 값도
  KOSPI/KOSDAQ/KOSDAQ GLOBAL/KONEX 뿐. → ADR-0003 갱신 불필요.
- **`Code` 재사용 증거 없음.** 1년 이상 공백 후 재등장한 코드 32개 중 이름이 바뀐 12건 검토 —
  보통주(끝자리 0)는 전부 알려진 사명 변경·합병(한빛→우리은행, 진로산업→JS전선,
  인포피아→오상헬스케어, 한주금속→한주라이트메탈 등)이고, 나머지는 우선주·신주인수권 계열
  코드(끝자리 ≠0)라 어차피 유니버스 제외 대상(ADR-0003)이다. `Code` 를 종목 식별 키로 써도 된다.
  단, 장기 거래정지 후 재상장 구간(공백)이 존재한다는 점은 백테스트 엔진이 알아야 한다.
- **거래정지/무거래**: `Amount==0` 전 구간 5.60%, 2017+ 구간 4.24%. `Amount==0 ⇔ Volume==0`
  (100% 일치). 무거래일 1,000일+ 종목 존재(장기 정지). **처리 정책(제안, BORB-31 에서 확정)**:
  당일 `Amount==0` 종목은 진입 불가로 스크리닝에서 제외, 보유 중 정지되면 다음 거래 가능일까지
  청산 불가로 처리. 정책 확정은 백테스트 엔진 ADR 몫.

**남은 주의점:**
- 데이터 갱신 지연 — 마지막 거래일이 실제보다 1~2 영업일 늦다. 백테스트엔 무관, 실시간 운용엔 부적합.
- 액면분할/병합 back-adjust 는 **구현 완료**(ADR-0006, `api/main.py`). 배당은 미보정.

## 2. 종목 마스터 (point-in-time)

marcap의 날짜별 스냅샷에서 파생. 별도 테이블로 둘지, 패널에서 그때그때 뽑을지는 구현 시 결정.
필요 필드(안): `Code, Name, Market, 최초등장일, 최종등장일(≈상폐일 근사), 상장상태`.

## 3. 수급 (외인/기관/개인) — **TBD (ADR-0002)**

소스 미정. 유력 후보 KIS `investor-trade-by-stock-daily`. 확정 시 채운다. 예상 필드(안):
`Date, Code, 외국인_순매수, 기관_순매수, 개인_순매수, (기관 세부 분류)`.
파생 신호: 외인 5일 누적 순매수 / 시총 (§4.3 Tier 1).

## 4. 재무 (OpenDART) — **백필 대기 (BORB-41)**

> **상태: 스크립트 준비 완료, 실행 대기.** `scripts/backfill_dart.py` — `DART_API_KEY`
> 발급(https://opendart.fss.or.kr) 후 실행. 아래 스키마는 **안(案)** — 백필 후 실측으로 정정한다
> (§1 marcap 때도 초안과 실측이 여러 곳 달랐다).

- **소스**: OpenDART "단일회사 전체 재무제표"(`fnlttSinglAcntAll`), OpenDartReader `finstate_all()`.
  연결(CFS) 우선, 연결 미작성 법인은 별도(OFS) 폴백 — `fs_div` 컬럼으로 구분 보존.
- **저장**: `data/derived/dart/{종목코드}/{연도}Q{분기}.parquet` (분기당 1파일, 커밋 안 함 — 저장 규약 참조).
  분기↔보고서 코드: Q1=11013(1분기), Q2=11012(반기), Q3=11014(3분기), Q4=11011(사업보고서).

### 스키마 (안)

| 컬럼 | 의미 | 비고 |
|------|------|------|
| rcept_no | 접수번호(14자리) | 원본 |
| **rcept_dt** | **접수일자(YYYYMMDD)** | `rcept_no` 앞 8자리에서 파생 — **as-of 키** |
| account_id / account_nm | 계정과목 ID(XBRL 표준계정) / 명 | 예: `ifrs-full_Revenue` / 매출액 |
| account_detail | 계정 상세 | 자본변동표 등에서 사용 |
| sj_div / sj_nm | 재무제표 구분/명 | BS/IS/CIS/CF/SCE |
| thstrm_nm / thstrm_amount | 당기 명칭 / **당기 금액(원)** | 문자열로 올 수 있음 — 실측 후 타입 확정 |
| thstrm_add_amount | 당기 누적 금액 | 분기 보고서에서 사용 |
| frmtrm_nm / frmtrm_amount | 전기 명칭 / 금액 | |
| currency | 통화 | 대부분 KRW |
| stock_code / corp_code | 종목코드(6) / DART 고유번호(8) | 스크립트가 부여 (marcap `Code` 와 조인 키 = stock_code) |
| bsns_year / quarter / fs_div | 사업연도 / 분기 / CFS·OFS | 스크립트가 부여 |

### as-of 원칙 (look-ahead 금지 — 방법론 가드레일)

재무제표의 **대상 기간과 공시일은 다르다.** 12월 결산 법인의 사업보고서(Q4)는 통상 이듬해
3월에야 접수된다. 백테스트에서 재무 데이터는 반드시 **`rcept_dt`(접수일) 이후에만** 신호 계산에
쓸 수 있다 — 대상 기간 종료일(예: 12-31) 기준으로 쓰면 아직 공시되지 않은 숫자를 미리 보는
것이다. as-of 조인 키가 `rcept_dt` 인 이유. ("신호 계산 시점 < 체결 시점" 불변식의 재무판.)

**남은 결정(백필 후):** 정정공시(기재정정)는 새 `rcept_no` 로 다시 접수된다 — 최초 공시본을
쓸지(엄격한 as-of), 최종 정정본을 쓸지 정책 필요. 금액 컬럼 타입·결측 실측 확정도 백필 후.

## 저장 규약

- 원본은 커밋하지 않는다 (`data/` `.gitignore`). marcap은 clone, 나머지는 로컬 적재.
- 중간 산출물은 parquet. 파티션 키는 연도 또는 연·월 (구현 시 결정).
- 컬럼명은 소스 원본을 유지한다. (초안에 있던 "오타 컬럼 `ChagesRatio` 정규화" 규약은 **불필요** —
  실제 데이터는 `ChangesRatio` 로 정상이다.)
