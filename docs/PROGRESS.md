# 작업 대장 — 레이어별 한 줄 기록

> 완료 작업을 레이어로 구분해 한 줄씩만 남긴다. 상세는 ADR·Linear(BORB)·git log.
> 정본은 이 파일과 docs/adr/ — Linear 는 추적용 사본이다.

## Layer 1 — 데이터

- marcap 로더 + 무결성 검증 (ADR-0002, e5b7e3e)
- 종목코드 6자리 정규화 + 유니버스 제외: KONEX·스팩·우선주·리츠·관리종목 (ADR-0003, c77c77c)
- 수정주가 보정 — 액면분할/병합 back-adjust, 정본 layer1/adjust.py (ADR-0006, 9faf15e→d17271b 이관)
- 잔여 점검 3건 종료: ETF/ETN 없음·코드 재사용 없음·거래정지 Amount==0⇔Volume==0 (BORB-32, 4a5fdac)
- 수정주가 일봉 사전 계산 — scripts/build_adjusted.py 생성 · derived.py 읽기 창구 (보정 정본은 adjust.py 그대로)
- marcap 갱신 지연분 보충 — 네이버 종목시세로 최신 거래일까지 (BORB-44, ADR-0002 개정)
  - marcap 이 정본. 거래대금 근사·point-in-time 아님·Dept 없음 → **표시 전용, 백테스트 ❌**
- 업종 데이터 실측 — KIS 업종 일자별지수가 **2017-01 까지, 호출당 100 거래일, 날짜 지정(point-in-time ⭕)**
  (ADR-0016, `scripts/kis_probe_industry.py`). 코스피 종합을 yfinance 와 대조해 소수점까지 일치
  - **KIS 에 테마 API 는 없다** (국내주식 156개 전수 확인). 주는 건 거래소 표준 업종 138개
  - 상승/하락 종목 수(`ascn_issu_cnt`)는 `output1`(오늘)에만 — **과거 구간의 "절반 이상 상승"은 못 받는다**
  - 상폐 종목엔 지수 업종 코드가 없다(표준산업분류만 남음) → 그대로 쓰면 생존 편향이 업종 조건에만 생긴다
  - **자릿수 함정** — 종목 필드 `013`(3자리) vs 지수 API `0013`(4자리). 3자리로 부르면 실패가 아니라 **빈 응답**

## Layer 2 — 신호 (LLM)

- (미착수 — Backtest Phase 2 에서 시작)

## Layer 3 — 전략

- screening.py 골격 — 임계값 전부 placeholder (b96f9ba)
- 케이스 검사기 전략 오버레이 배관 — STRATEGIES 레지스트리, ma_cross 는 예시 (0261ddb)
- 조건검색 엔진 — 키움 [0150] 식 4카테고리 20조건, 결정론적 pandas (355d593)
- 조건검색 v2 — 룩백에 수정주가 back-adjust 적용 + TA-Lib 패턴분석 11종 (BORB-41 ①)
- 전략 카탈로그 개편 — 조건검색과 같은 레지스트리·파라미터 스키마, ma_cross 고정값(5/20) 제거 (ADR-0009)
- 피보나치 되돌림 오버레이 — 베이스→신고가 앵커, 파라미터 전량 요청 주입 (BORB-42, ADR-0009)
- 전략 1호 계산 코어 — 급등 앵커·분할 레벨·앵커 VWAP (ADR-0011, BORB-50/51)
  - `surge.py` 급등 파동(시가→최고가) + 52주 신고가 앵커, `as_of` 로 과거 시점 재현
  - `entry_levels.py` 분할 매수/매도 목표가. 차수는 체결 순서, 매수는 가격 내림차순 강제
  - `avwap.py` 앵커 VWAP — **Amount÷Volume 실값**(근사 아님), back-adjust 와 자동 정합
  - `tick_size.py` KRX 호가단위(2023 개편) + 라운드 피겨
  - 적대적 검증 9회(모듈3 × 렌즈3)로 차단급 4건 발견·수정 → `test_strategy_core_regressions.py`
- 갭 상승 앵커 확정 — **갭 전날 시가** ("갭도 상승의 일부", 오너 2026-08-05). 코드 변경 없음, ADR-0011 미결 종결
- 조건검색 이벤트 조건에 '이내(within)' — 신고가·신저가·갭상승 "최근 X일 이내 발생" (골든크로스와 동일 방식)
  - 룩백 상한 260→520 거래일 — "52주 신고가를 최근 1년 내 찍은 종목" 검색 가능
- 조건검색·시뮬레이션 기본 기준일 = **전날(마지막 완결 거래일)** — 장중 보충 데이터로 조건 평가 ❌ (오너 지시)
- 전략 1호 시뮬레이션 — `POST /api/simulate` + ③ 시뮬레이션 탭 (ADR-0011 §4, BORB-52)
  - 앵커·분할 목표가·체결 마커(▲▼차수)·앵커 VWAP·체결 내역(수량·평단·실현/평가손익, 비용 미포함)
  - **손절** — 평단 −% 또는 지지저항(앵커 VWAP/급등 시작가/직접가) **±N호가** (`tick_size.shift_ticks`)
- 전략 1호 파동 = **상승장 사이클** (저점 `find_cycle_low` → 저점 이후 최고 High) — "급등" 개념 폐기 (ADR-0013 + 당일 개정, 오너 확정)
  - `window`·`min_gain_pct` 입력 제거, AVWAP 앵커 = 사이클 저점, 손절 급등 시작가 → 사이클 저점 이관
  - 매도 기준가 응답·화면 명시(`sell_basis_price`) — "평단 +10%가 왜 40만" 오해 재발 방지
  - ③ 차트 요소별 표시 필터(줌 유지) + **전략 오버레이(차트 탭)도 종류별 필터** + 수평선 라벨을 선 아래로 (오너 지시)
  - 기준일 기본 = 오늘(검색·시뮬 공통) · 종목 전체 이력 캐시로 시뮬 13s → 0.06s
  - 케이스 검사기 피보 오버레이 = 같은 사이클 정의(`fibonacci.py` 재작성, 기준일 왼쪽만) — 베이스 탐지 완전 폐기
  - ② "차트에 적용" 버튼 복원(pickStrategy 호출부 유실이 "차트에 안 뜸"의 원인) + 오버레이 실패 화면 표시 + 매도 기준점 ③ 노출
- 목표가 = **지지/저항 수평선**(스윙 피벗+군집, `support_resistance.py`) ± 호가 오프셋 — 라운드 피겨·앵커 VWAP 폐기 (ADR-0014, 오너 확정)
  - 근접 판정·라운드 허용폭 입력 삭제 · 파동/지지저항 파라미터 정본 = ② 진입 기법(fib_retrace) · ③ = 실행 전용(분할 카드 삭제)
  - 피벗은 우측 span 봉 확정 필요 — 기준일 시점에 없던 선이 안 생김(look-ahead 구조 차단)
  - 화면 용어 쉬운 말: "고점·저점 기준" / "같은 선 폭" (피벗·군집 ❌, 오너 지시)
  - 목표가 못 걸면 200+warnings 로 강등 — 그릴 수 있는 건 다 그림 (오류만 띄우고 빈 화면 ❌)
  - 시뮬 이력 로드 6.5s → **0.38s** — derived(사전 계산본)+꼬리 재사용, 느린 길과 OHLC 완전 일치 검증
  - 미해결: 추세선(대각) 지지저항 — 주문 정정 로직 설계 후 (오너 케이스 확인 예정)
- 사이클 저점 = **반등 확인된 바닥만** — 하락 진행 중엔 직전 상승장 유지 + `falling` 경고 (삼성 -49.5% 하락 중 실측으로 확정)
- 피보나치 = 그리기 도구식 0~100% 전체 레벨 + 가격 병기 · 지지저항은 사이클 구간·피보 구간 안만 (선 217→5개)
- **④ 백테스팅 탭** — 전략 1호 전수 검사 v1 (ADR-0015, `strategy_one.py` + `POST /api/backtest`)
  - ADR-0007 골격 재사용(3분할·Test 1회 가드·비용·N<30), 세팅은 기준일 왼쪽만·체결은 오른쪽만
  - 실측: 검증 2024 유니버스 23종목 → 거래 12건 2.3초
- 복합 조건 **신고가+거래대금** — 돌파일에 거래대금 X억 이상(같은 봉), ①·④ 공용 (오너 정의)
- ③ 화면 정리 — 차트 위아래 띠(요소 필터 칩·결과 요약)를 **왼쪽 사이드로** 이관, 차트가 패널 높이 전부 사용
  - 중복이던 요약 표(저점/고점/매도 기준가/체결 마커) 삭제 — 같은 내용이 요약 문단에 있다
  - 지지저항 표시 = **피보 5선에 각각 가장 가까운 선만**(`support_resistance.nearest_per_target`, 최대 5개)
    — ②·③ 공통. **목표가가 걸릴 후보는 안 줄인다**(화면 정리하려다 주문 가격이 바뀌면 안 됨)
  - 화면 용어 '상승장' 폐기 → **'피보나치 구간'** (오너 확정 2026-08-06)
- ③ 오버레이 실종 **해결** — 원인은 Pro `setSymbol` 이 Solid 시그널 setter 라 **같은 객체
  참조는 === 비교로 무시**되던 것(레이아웃 이관 전엔 결과 요약이 차트 아래 생기며 컨테이너
  리사이즈 리페인트가 우연히 그려줌). `applySimulation`·`refreshStrategy` 의 다시 그리기를
  window resize 디스패치로 교체(줌도 유지) — Playwright 재현 스크립트로 전후 실측
- ② 분할 매수 빠른 버튼에 23.6% 추가 — 차트 피보 5선(FIB_RATIOS)과 동일 목록 (오너 지적)
- ② 파동·지지저항 입력칸 제거 — 전략 1호 고정 정의(STRATEGY_ONE_WAVE)로 (오너: "시작점·고점은
  자동으로 구하는 건데 입력값을 왜 내가 만지나"). ③·④는 정의값을 그대로 쓴다
- **지지저항 = TradingView "Support Resistance Channels" 포팅** (ADR-0014 개정 2, 오너 지시:
  "임의로 계산식 만들지 말고 인터넷에서 찾아" — 바퀴 재발명 3번째 질책)
  - 피벗 탐지(좌우 N일)는 원래 표준과 동일했고, 비표준이던 고정 % 군집·평균 단일선을 폐기
  - 존 폭 = 최근 300봉 가격폭 × 5%(가격대 자동 적응) · 강도 = 피벗×20 + 터치 봉×1 ·
    겹침 제거 후 강도순 최대 5존. 피벗은 우측 prd 봉 경과 후 확정(point-in-time, 원본 포팅본과
    다른 유일한 부분 — 원본들은 일괄 계산이라 look-ahead)
  - 파라미터 sr_prd/sr_channel_width_pct/sr_loopback/sr_min_strength/sr_max_channels —
    요청 데이터로만(ADR-0009). 화면 존 = 반투명 띠 + 중앙 점선
  - 목표가 스냅 = **존 중앙** (오너 확정 2026-08-06)
- **전략 저장 회귀 + 캡슐화** — ② 입력칸 숨김 후 저장 검증만 옛 가정(입력 필수)에 남아 저장 불가.
  전략 1호 정의를 `strategyOne.ts` 단일 정본으로 캡슐화(오너 지시), 화면·요청·검증이 공유 (6c7ae15)
- **전략 패널 구조 리팩토링** (오너 승인: "컴포넌트화 + 파일 분할") — 1,522줄 StrategyPanel →
  얇은 셸(141) + 화면별 Screen/Strategy/Sim/BacktestStep + `components/ui.tsx`(Card·KV·MsgLine·Chips).
  동작 변경 제로 — 전후 스크린샷 픽셀 대조 diff 없음, 전 파일 800줄 규칙 준수 (812f8eb)
- ② "차트에 적용"·"해제" 버튼 삭제 (오너 결정) — ②는 설정만, 확인은 ③에서.
  차트 탭 전략 오버레이 입구 폐기(bus.pickStrategy 생산자 없음, ChartPanel 리스너는 잔존)
- **미해결(다음 세션 최우선)**
  - 피보나치 **시작점 정의** — "상승장 시작"은 폐기(오너). 한 번도 −drop% 를 안 맞은 종목은
    시작점이 수십 년 전이 된다(SK하이닉스 −60% → 2008-11-25 5,770). 오너 안 = "지금 가격으로 봤을 때
    과거 차트가 평평해 보이는 지점". 실측 후보: **고점의 10% 컷 + 그 안에서 −30% 사이클**
    → SK하이닉스 2025-09-10 293,000(눈대중 30만) · 삼성전자 2024-11-14 49,900(눈대중 5~6만). 오너 확정 대기
    (2026-08-06 재문의 — "보류, 버그부터". 오버레이 복구됐으니 다음은 차트 보며 확정).
- **헤더 1줄 통합** — 브랜드·메뉴·검색·지수·시세 한 줄(44px), 창관리는 ⋯ 팝업. 1366×768 실측 헤더+탭줄 ≈10%
  - 1차 시도(100vh+sticky)는 도크 패널 안에서 차트를 짓눌러 폐기 — ③ 차트는 그리드 행으로 패널 높이를 그대로 채움
- **종목 로드 경합 수정** — 차트 초기화 직후 showSymbol 호출 시 늦게 온 이전 종목 응답이 덮음(제목/캔들 불일치
  → y축 어긋나 오버레이 전멸). 초기 종목은 `initialSymbol` prop 으로 — ③·차트 탭 공통
- 그리기 도구 '선분' 라벨 — 시작점 "가격·날짜", 끝점 "가격 (변화율%)·날짜" (내장 오버레이 재등록, 트레이딩뷰식)
- ③ 대표 종목 삼성전자 → SK하이닉스(000660) (오너 지시)
  - ① 조건검색 증권사식 단일 계층(카테고리→조건, 빠른선택·드롭다운 이중 구조 삭제) + 신고가/신저가 한 항목·구분 라디오 (오너 지적)

## Layer 4 — 실행/백테스트

- 거래비용 정액률 다단계 (ADR-0004, 3690d66)
- 백테스트 엔진 골격 — 신호(t)→t+1 시가 체결, 거래정지 연기, 3분할 가드, N<30 플래그 (ADR-0007, d17271b)
- 슬리피지 제곱근 충격 모델 + 유동성 하한 공식 ADV≥Q·(k/s)² (ADR-0004 갱신, 355d593)
- 멀티종목 러너 — 조건검색식으로 유니버스 선별 → 전략 신호 → 종목별 체결 → 집계 (ADR-0007/0009)
  - v1 한계 명시: 유니버스 선별 1회(주기 리밸런스 없음), cum_net_return 은 순차 복리(포트폴리오 자본곡선 아님)
- KIS 브로커 어댑터 — 토큰 발급·캐싱, rt_cd 실패 판정, HTS조회상위20 (ADR-0010, 51c91a1)
  - **조회 전용**. 주문 전송·hashkey 없음 — 모의투자 확보 후 새 ADR (CLAUDE.md 단계 6)
  - 조회상위는 "지금 이 순간"만 준다 → point-in-time 재현 불가, **백테스트 ❌**. 쓰려면 지금부터 시계열 축적
  - 시장구분 코드 확정 — `J`=유가증권(ETF 포함), `Q`=코스닥(글로벌 포함). KONEX 는 미관측 (BORB-47)
  - 호출 제한 미대응 — 프로브 연타만으로 `EGW00201 초당 거래건수 초과`. 백필 전에 스로틀 필요

## 도구 — 케이스 검사기 웹 (레이어 밖, 탐색용)

- FastAPI + Vite/React + KLineChart Pro 차트, 수정주가·주/월봉·ko-KR (ADR-0005, 53e772a→0261ddb)
- HTS 멀티뷰 셸 — dockview 도킹(탭·플로팅·팝아웃), ECharts 시장맵, 관심종목 (ADR-0008, c1fad55)
- 화이트 테마 + 키움식 조건검색 UI + 워터마크 제거 (355d593)
- 소프트 UI 전면 적용 — KIS 앱 톤 토큰·2행 헤더(통합검색+지수 티커)·스파크라인·시세포착 감시 목록 (ADR-0008 개정)
- 검색바 CSS 특이도 충돌 수정 + 한 글자마다 실시간 검색(시총순 정렬) + 우측 종목 드로어 (ADR-0008 개정 2)
- 홈 지수 보드 — 코스피/코스닥 장중 5분봉 + 투자자별 순매수(네이버 지수 trend, 억원) + 순위 5종 (ADR-0008 개정 2)
- 전략 3단계 분리 — ① 조건검색식 CRUD ② 매매전략 ③ 감시. 조건 0개 = 전체 종목 (ADR-0009 개정 2)
- 첫 실행은 홈 탭 하나만. 기본 레이아웃 변경 시 LAYOUT_KEY 를 올린다 (ADR-0008 개정 3, cc862c0)
- 시세포착 감시 제거 — 전략 = ① 조건검색식 + ② 매매전략 2단계 (ADR-0009 개정 3, BORB-53)
- 조건검색식 화면 = 입력(조건 만들기) / 결과물(검색식) 분리 + 요동 제거 (ADR-0008 개정 4, BORB-49)
- 넓은 패널 대응 = **컨테이너 쿼리** (뷰포트 ❌ — dockview 패널 폭은 뷰포트와 무관)
  - 가로 스크롤바 원인은 select 의 min-width:0 누락, 스크롤바 2개는 .dv-groupview 의 border 1px
- 종목 확인 동선 = ① 종목선정 일원화 — ②의 "이 조건으로 종목 보기" 제거, 종목 상세 ① 이동 (BORB-54)
  - 검색 결과 리스트 — 미니 캔들(30일)·코스피/코스닥 배지·네이버 테마(`themes.py`, 표시 전용·백테스트 ❌)
- 전 차트 캔들봉 통일 — 홈 지수 5분캔들·시장지표 일봉캔들·티커/관심종목/드로어 `MiniCandles`, Sparkline 삭제 (BORB-55)
- 실시간 시세 — 네이버 폴링(`quotes_rt.py`, 다종목·지연0) `/api/quotes` 병합, 관심종목·드로어·종목상세 10초 갱신
  - **전략 검색·시뮬레이션은 전날 기준 유지** (실시간은 표시에만 — 오너 지시)
- 차트 탭 전환 시 확대 누적 버그 수정 — 숨김(0px) 리사이즈 무시 (ProChart)
- 시장맵 finviz식 개편 — ECharts(줌 시 재배치) 폐기, 자체 squarified SVG(`mapLayout.ts`) (585c499)
  - 줌 = transform 만(레이아웃 불변, 안 보이던 글자만 LOD 로 나타남) · 코스피/코스닥 필터
  - 업종 그룹 + 헤더 띠 = 네이버 업종 79종(`industry.py`·`naver_groups.py`, 표시 전용·백테스트 ❌)

## 결정/폐기

- ADR-0001 폐기 — 종가 매매 고정 전제 제거, 진입 방식은 전략 확정 후 (52e8aa9)
- 수급 데이터 소스 = **KIS `FHPTJ04160001`** 종목별 투자자매매동향(일별) (ADR-0012, BORB-33/34/35/36)
  - 실측: 요청일 20170102 에 20161121~20170102 응답 → **백테스트 구간 2017~ 을 덮는다**. 30 거래일/호출
  - 외국인(등록·미등록)·개인·기관 8종 세부를 수량·금액 양쪽으로. **날짜 지정 호출이라 point-in-time ⭕**
  - 키움 `ka10008` 은 예비. 통짜 데이터셋은 없음(pykrx = CLAUDE.md 금지 의존)
  - ~~상폐 종목 조회 가능 여부~~ **해소 (2026-08-05, ADR-0012 미해결 2번)** — 6년 전 상폐까지 정상 응답
  - 미결: 백필 범위(전 종목 22만 호출 ≈ 30h vs 상위 N) — 오너 결정
- 업종/산업 모멘텀 = **C안** (자체 계산 + KIS 업종 지수 대조) (ADR-0016, BORB-68, 오너 결정 2026-08-07)
  - 무리 = 생존 종목은 지수 업종, 상폐 종목은 표준산업분류로 메꿈 — **두 체계 매핑표가 실제 작업량**
  - 네이버 테마 크롤링은 **화면 표시 전용 유지** (오늘 명부만 주므로 과거 구간에 쓰면 look-ahead)
  - 재무로 업종 분류 ❌ — 숫자로는 업종을 판별 못 하고 분류는 이미 주어져 있다. 재무는 종목 품질 필터로만
- VIX ≠ 공포지수 — 표시 이름 정정. 한국형 공포·탐욕 지수 자체 계산은 BORB-59 (오너 지적 2026-08-05)
- 재무=OpenDART·패턴=TA-Lib 조사 완료 (BORB-41) — 패턴은 반영 완료, 재무는 OpenDART 백필 대기
  - 백필 스크립트(scripts/backfill_dart.py)·스키마 안(DATA_SCHEMA §4) 준비 완료. DART_API_KEY 발급 후 실행하면 실측으로 정정
  - 미결: 정정공시(기재정정) 최초본/최종본 중 무엇을 쓸지 — as-of 정책 결정 필요
- 전략·조건은 데이터로 정의, 숨은 하드코딩 금지 (ADR-0009, 오너 지시)
