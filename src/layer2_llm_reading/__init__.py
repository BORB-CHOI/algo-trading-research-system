"""Layer 2 — 글 해석. **LLM 이 들어가는 유일한 자리** (2단계~).

지금은 비어 있다. 정량 신호(피보나치·지지저항·파동)는 layer3 이 결정론적 코드로 직접
계산한다 — 여기가 아니다. 예전 이름 `layer2_signals` 는 "정량 신호도 여기 있나?"로
읽혀서 2026-08-16 에 바꿨다.

여기 들어올 것: 뉴스·공시·재료 글을 읽고 **등급(ordinal)** 을 매기는 일뿐이다.

## 왜 칸을 비워두고 지키나

의존 방향이 한 방향이다: `layer4 → layer3 → layer2 → layer1`.
LLM 이 여기 들어와도 **layer3(매매 결정)을 부를 수 없다.** CLAUDE.md 의
"LLM/MCP 가 BUY/SELL·포지션 크기·손절 라인을 결정하지 않는다"를 코드 구조로 못 박는
자리다. 폴더를 없애면 그 경계가 문서에만 남는다.
"""
