"""4단계 — LLM 이 글과 통계를 읽고 해석한다 (ADR-0024).

여기가 **LLM 이 들어가는 유일한 자리**다. 지금은 비어 있다.

## 하는 일 두 가지

1. **물어보면 답한다.** 1·3단계 원본과 2단계 통계를 참고해 필요한 작업을 하고 답한다.
   일회용 코드를 짜서 돌려도 되고 데이터를 써도 된다.
2. **시나리오를 짠다.** 그 시점의 국면(시황·투자심리·주도테마·수급·차트)을 읽고
   2단계 통계와 묶어 매매 시나리오를 낸다.

## 지켜야 할 것

- **정량 근거 없이 판단하지 않는다.** 글만 읽고 매수·매도를 말하는 것이 금지된 것이다.
- **여기서 5단계(`layer5_execution`)를 부르지 않는다.** LLM 은 시나리오를 글로 낼 뿐이고,
  주문으로 바뀌는 것은 사람이 승인한 뒤 5단계가 한다. 시험으로 막는다.
- LLM 이 낸 계획도 **2단계 백테스트를 통과해야** 쓴다 — 승률·손익비가 실제로 나아졌는지
  숫자로 보여야 한다.
"""
